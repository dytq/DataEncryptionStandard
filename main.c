/* Implémentation de DES juste pour le fun de coder ;-) */

/* Bibliothèques */
#include <stdlib.h>
#include <stdio.h>
#include <string.h>

#include "bloc.h"
#include "interface.h"
#include "cle.h"
#include "feistel.h"

/* ------------- */

int main(int argc, char ** argv) {
	//~ parsing_terminal(argc,argv);
	/*
	bc64 * blocs = convertir_message_64_bits("testtesdtesttest");
	bc64 test = 3;
	printf("%lX\n",permutation_initial(test));
	bc64 test2 = permutation_initial(test);
	printf("\nPermutation initial: %lX\n",test2);
	printf("\nPermutation final: %lX\n",permutation_final(test2);
	free(blocs);*/
	swap_bloc(54,54);
	exit(EXIT_SUCCESS);
} 